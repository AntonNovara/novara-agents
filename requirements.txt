# API Gateway
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Agent Orchestration
langgraph>=0.2.53,<0.3.0
langchain-core>=0.3.27,<0.4.0
langchain-anthropic>=0.3.0,<0.4.0

# Data Validation
pydantic==2.10.3
pydantic-settings==2.6.1

# Environment
python-dotenv==1.0.1

# PDF Parsing (Operations Agent)
pdfplumber==0.11.4

# File upload support (multipart/form-data)
python-multipart==0.0.20

# Utilities
httpx==0.28.1
structlog==24.4.0

# Voice Agent (direct Anthropic SDK for SSE streaming)
anthropic>=0.40.0

# Google Calendar API (book_appointment tool)
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0

# MCP Server (tools/mcp_server.py) – exponiert Novara-Tools für Kunden-CRMs
mcp>=1.6.0,<2.0.0

# Baustellen-Voice-Assistant (agents/field_worker_agent.py, utils/pdf_generator.py,
# main.py POST /api/v1/webhook/whatsapp): Twilio-WhatsApp-Webhook-Verarbeitung,
# Regiebericht-PDF-Erzeugung, Audio-Format-Handling für Sprachnachrichten.
# pydub braucht zur Laufzeit zusätzlich das externe ffmpeg-Binary (siehe Dockerfile).
twilio==9.11.1
fpdf2==2.8.8
pydub==0.25.1

# Speech-to-Text für WhatsApp-Sprachnachrichten (main.py _transcribe_audio(),
# whisper-large-v3 über die Groq API).
groq==1.7.0

# Rate-Limiting für POST /api/v1/chat/landing (main.py) — öffentlicher,
# unauthentifizierter Endpoint, siehe CLAUDE.md "Bekannte Einschränkungen".
slowapi==0.1.9

# Persistenter Store (core/db.py) für consent/customer_state/lead_capture/
# sequence_scheduler — Postgres in Produktion (Railway-Plugin), SQLite lokal.
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
